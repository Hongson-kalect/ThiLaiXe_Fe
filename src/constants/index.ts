import { AUTH } from "./auth";
import { API_STATUS } from "./api.status";
import { LOCALSTORAGE } from "./localStorage";

const constants = { AUTH, API_STATUS, LOCALSTORAGE };

export default constants;
